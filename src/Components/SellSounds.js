import { Component } from 'react';

export default class SellSounds extends Component {

    render () {
        return (
            <form>
                <input type="file" />
            </form>
        )
    }
}